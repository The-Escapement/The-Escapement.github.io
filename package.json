{
  "name": "the-escapement",
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i src/tailwind.css -o dest/style.css --minify",
    "watch:css": "tailwindcss -i src/tailwind.css -o dest/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "postcss-cli": "^10.0.0"
  }
}
